<%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.label(:completed?) %>
  <%= f.check_box(:completed) %>
  <%= f.button :submit, wrapper_html: { class: 'btn-primary' } %>
<% end %>

